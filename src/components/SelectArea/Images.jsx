import React from "react";

function Images() {
  return (
    <div>
      {/* {results?.results?.map(({ title }, index) => (
        <div key={index}>{title}</div>
      ))} */}
    </div>
  );
}

export default Images;
